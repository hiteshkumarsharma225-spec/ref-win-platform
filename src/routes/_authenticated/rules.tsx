import { AppShell } from "@/components/AppShell";
const rules = ["Use only one account and keep your login details private.","Do not use bots, automation, collusion, or manipulated results.","Submit results only after the game has actually finished.","Keep screenshots or other evidence when a result is disputed.","Use the support channel for genuine disputes and account issues."];
export default function RulesPage(){return <AppShell><div className="space-y-4 pb-24"><h1 className="text-xl font-bold">Rules & Fair Play</h1><div className="rounded-2xl border border-white/10 bg-white/[0.04] p-5"><ul className="space-y-4">{rules.map((r,i)=><li key={i} className="flex gap-3 text-sm leading-6 text-white/70"><span className="grid h-6 w-6 shrink-0 place-items-center rounded-full bg-amber-400/15 text-amber-300">{i+1}</span><span>{r}</span></li>)}</ul></div></div></AppShell>}